import type { AxiosInstance } from 'axios'

// eslint-disable-next-line unused-imports/no-unused-vars
export default (instance: AxiosInstance) => ({
  // Эндпоинты которые нельзя явно класифицировать
  // ...
})
